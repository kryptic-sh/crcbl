[package]
name = "crcbl-phys"
description = "Crucible physics: queries, forces, CCD"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
crcbl-ecs = { workspace = true }
glam = { workspace = true }

# Tests only: the churn suite drives its operations from an index hash, which is
# `crcbl-core`'s. The crate itself does not need it — it reaches the ECS, and the
# ECS reaches core.
[dev-dependencies]
crcbl-core = { workspace = true }

[lints]
workspace = true
