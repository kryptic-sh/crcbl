{
  "comment": "Asset keys the shim pre-loads through __crcbl_web_fetch_* before the demo boots. quarry has none: the face is generated from a hash of each lattice coordinate at start-up rather than loaded, it draws no sprite art at all — sample rule 11 exempts it by name — and its shaders and font atlas are compiled into the wasm module. The list is the seam, not the content.",
  "keys": []
}
