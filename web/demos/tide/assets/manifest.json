{
  "comment": "Asset keys the shim pre-loads through __crcbl_web_fetch_* before the demo boots. tide has none: the courtyard and the three stub rooms are described in Rust rather than loaded, it draws no sprite art — its charter exempts it from sample rule 11 on lantern's ground — and its shaders and font atlas are compiled into the wasm module. The list is the seam, not the content.",
  "keys": []
}
